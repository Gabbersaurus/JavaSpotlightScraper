package Scraper;

/**
 * Defines the type of the callback
 *
 * LOG = a log message from the scraper
 * FINISH = the scraper is done scraping
 */
public enum ScraperCallbackType {
    LOG,
    FINISH
}